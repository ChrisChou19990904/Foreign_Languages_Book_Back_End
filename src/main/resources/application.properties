# --- Server Configuration ---
server.port=8080
spring.application.name=LanguageLearningBookStore

# --- 1. Supabase Database Connection (PostgreSQL) ---
# ???? <HOST>, <PASSWORD> ??????????
spring.datasource.url=jdbc:postgresql://db.rwbifniotmdvolewoana.supabase.co:5432/postgres?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=A3wxaq41h7c3

# --- 2. HikariCP Connection Pool Optimization ---
# HikariCP ? Spring Boot ??????????????????????
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000 
spring.datasource.hikari.connection-timeout=20000 

# --- 3. JPA / Hibernate Settings ---
# ???????? JPA ??
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.default_schema=public

# DDL Auto: update ???????????? Entity ???? Schema?
# ??????????? validate ? none???? Flyway/Liquibase ???????
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# --- 4. Logging Configuration ---
# ?????????????? JPA ??? SQL ??
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE

# --- 5. Error Handling / Security (Basic Setup) ---
# ???????????????
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=always

# ?? Spring Boot ?????????????? Spring Security + JWT ??????
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# ?????????????? Base64 Key
application.security.jwt.secret-key=ODhDNVg3dER1Z1E4V21rMmdWc3lQdkt0Y3JqZEdhSExoY1hXcG5yWThMOUU3PQ=

# ? 2. ?? Token ???? (????? milliseconds)?
# ???86400000 ?? = 24 ???
application.security.jwt.expiration=86400000
logging.level.org.springframework.security=DEBUG